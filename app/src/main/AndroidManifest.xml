<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="uz.mobilestudio.photo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Photo">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1320902743449340~2645854453"/>

        <activity
            android:name=".edit.EditImageActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SearchPhotoActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SearchActivity"
            android:exported="true" />
        <activity
            android:name=".activities.PhotoDbActivity"
            android:exported="true" />
        <activity
            android:name=".activities.PopularPhotoActivity"
            android:exported="true" />
        <activity
            android:name=".activities.PhotoActivity"
            android:exported="true" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>